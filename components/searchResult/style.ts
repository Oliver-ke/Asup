import {StyleSheet} from 'react-native';

export default StyleSheet.create({
    container: {
        flex: 1,
        marginTop: 20
    },
    textWrapper: {
     alignItems: 'center',
     width: '100%',
     marginTop: 50
    },
    text: {
        marginBottom: 10,
        fontSize: 16
    },
    textResult: {
        marginLeft: 10,
        fontSize: 18,
        fontWeight: "bold",
        color: 'rgba(0,0,0,0.7)'
    }
})